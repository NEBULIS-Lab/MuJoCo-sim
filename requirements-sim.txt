# Deliberately small simulation-only environment. Training libraries
# (torch, transformers, LeRobot, etc.) belong in a separate environment.
numpy==1.26.4
mujoco==3.8.1
robosuite==1.5.2
opencv-python==4.10.0.84
h5py==3.16.0
imageio==2.37.3
imageio-ffmpeg==0.6.0

